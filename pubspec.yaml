name: flutter_laundry_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  d_button: ^0.0.10
  d_info: ^0.2.2
  d_view: ^0.0.5
  d_input: ^0.5.1
  d_method: ^0.2.6
  dartz: ^0.10.1
  dynamic_button: ^0.0.7
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  google_fonts: ^4.0.5
  http: ^1.3.0
  intl: ^0.20.2
  shared_preferences: ^2.5.1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
